<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @group.description %>
</p>

<p>
  <strong>Icon:</strong>
  <%= image_tag(@group.icon) %>
</p>

<p>
  <strong>Owner:</strong>
  <%= current_user.name %>
</p>

<span> Total# <%= current_user.total_amount %> </span>

<%= render 'transactions' %>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
